---
type: chat-opener
created: YYYY-MM-DD
---

# Next chat: <build name>, Rome step N of 8

**Do not / closed:** <each closed decision, attributed: "do not X (user, YYYY-MM-DD: 'their words')"> · <each closed decision>.

**Read first, in order:**
1. `<path>`: <what it holds and why it is first>.
2. `<path>`: <what it holds>.
3. `<path>`: <what it holds>.

**Where we are:** <two or three lines: what shipped, what is evidenced, what is open>.

**Open:** <each open item with its state: done / owed to the user / declined>.

**Rules this chat set** (each attributed to a person and a file):
- <rule> (user, YYYY-MM-DD, `<file>`).

**First action:** <one action the reader may take on entry; never a send, attach or invite. If it depends on a number, a name or a ruling, that value is written here.>

**Model and mode:** <model, effort> · <orchestrator | single thread> · second reviewer <on | off>.

Session protocol (entry + retire): <link to the user's own project-session convention, or "none: read STATUS on entry, update it on exit">
